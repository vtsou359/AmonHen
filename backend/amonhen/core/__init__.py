"""Configuration and logging.

`config.py` is the single place anything reads the environment; nothing else in
the codebase touches `os.environ`. If you want to know what can be configured,
that file is the complete answer.
"""
