"""One module per group of endpoints.

    system.py     health, feed status, and the exported ontology
    incidents.py  the operational picture and per-fire dossiers
    layers.py     map layers as GeoJSON, plus the raster overlay catalogue
    danger.py     fire danger at a point or over a grid, and a what-if
                  spread calculator
"""
